/**
 * marked.min.js - Markdown 解析库（轻量版）
 * 为了方便离线使用，这里内联了必要的解析逻辑
 * 实际项目中可以引用 CDN: https://cdn.jsdelivr.net/npm/marked/marked.min.js
 */

// 使用 CDN 版本，此文件作为配置入口
window.BLOG_CONFIG = {
  marked: true
};
